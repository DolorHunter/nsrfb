作品使用指南

目录说明:
#   pic存放了一些测试图片
#   data为mnist中的训练图片库
#   model为训练的模型

程序说明:
#   forward.py为前向传播文件
#   backward.py为反响传播文件(运行即训练新的模型)
#   test.py为测试文件(读取当前精确度和训练轮数)
#   app_num_ch.py为单数字符识别程序
#   app_num_str.py为数字串识别程序
#   app.py为金融票据符识别程序
#   app_gui.py为金融票据识别程序的GUI


#[!]app_gui.py为作品( < - 直接运行此文件即可)